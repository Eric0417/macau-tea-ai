{
  "name": "macau-tea-ai-build",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && xcopy dist ..\\backend\\static\\ /E /Y /I"
  }
}